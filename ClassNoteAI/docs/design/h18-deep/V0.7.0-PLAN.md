# v0.7.0 Alpha · UI Overhaul Plan

把 H18 Deep prototype 移植到真實 React/TS/Tailwind ClassNoteAI app。
**分階段、每階段一個 alpha tag、隨時可用、絕不 big bang。**

---

## Goals

- 用 H18 視覺語言整套替換現有 UI（暖色 token 系統 / 字體層級 / 統一動畫）
- 引入 H18 chrome（traffic lights / 統一 top bar / 主題系統 / 通知系統）
- 設定頁重新組織對應 H18 ProfilePage 的 8 sub-pane 結構
- 每個 alpha 都是 **可發版** 的狀態，不留半途的混合視覺

## Non-goals (此版本)

- ❌ 引入 Inbox 系統（需要 reminders schema，留 v0.7.x patch）
- ❌ 引入 Calendar 排課系統（需要 schedule events schema，留 v0.7.x）
- ❌ ⌘K Command Palette（需要 index 服務，留 v0.7.x）
- ❌ AIDock ⌘J 浮動（需要新 AI panel 架構，留 v0.7.x）
- ❌ AI Page 全螢幕（含 chat history sidebar，留 v0.7.x）
- ❌ Concept hover RVBilink（需要 RAG concept 抽取，留 v0.8）
- ❌ NotesEditor canvas 模式（需要繪圖層，留 v0.8+）
- ❌ POverview 編輯/書信/精煉 三變體（先做 A 累積版即可）
- ❌ 後端 schema 改動（只動前端）

---

## 架構策略

### 樣式方案：CSS Modules + CSS Variables（**不用 Tailwind**）

決策過程見 `STYLING-DECISION.md`（如有）或 commit history。簡述：
- Tailwind v3→v4 的 framework drift 已咬過一次（v3 → v4 fidelity 問題）
- H18 設計大量非 scale 值 (14px, 22px, 0.18em 等)，Tailwind arbitrary
  syntax 反而醜
- CSS Modules 跟 prototype inline style 對應度 1:1
- CSS spec 不會 breaking change，5-10 年不再被框架咬
- Type safety 用 `typescript-plugin-css-modules` + `tokens.ts` 補足

### Token 系統：純 CSS Variables

```
H18_THEMES { light, dark }  (prototype 物件)
  ↓ 抄到
src/styles/tokens.css    :root { --h18-text: ... }   :root.dark { --h18-text: ... }
src/styles/tokens.ts     export const TOKENS = { color: { text: 'var(--h18-text)' } }
  ↓
.module.css 直接用 var(--h18-*)
.tsx 想動態取值用 TOKENS.color.text (TS type-safe)
  ↓
切 theme = document.documentElement.classList.toggle('dark')
所有元件自動跟，不需 prop drilling
```

### CSS Modules 寫法

```tsx
// Card.tsx
import s from './Card.module.css';
import clsx from 'clsx';

export function Card({ active, children }: Props) {
  return <div className={clsx(s.root, active && s.active)}>{children}</div>;
}
```

```css
/* Card.module.css */
.root {
  padding: 14px;
  border-radius: 8px;
  background: var(--h18-surface);
  border: 1px solid var(--h18-border);
}
.active {
  border-color: var(--h18-accent);
}
```

### Tailwind 移除策略

**不在 Phase 0 移除**。Tailwind 跟 CSS Modules 在 Vite 可以並存：
- 既有 28 元件**繼續用 Tailwind**
- 新元件**全用 CSS Modules**
- Phase 5 全部主頁面 port 完才刪 `tailwind.config.js` + `npm uninstall tailwindcss`

避免在 Phase 0 就 break 既有 28 元件。

### 響應式策略

設計是固定 1440×900 letterbox。實際 app 視窗大小可變。對應策略：

- `min-width: 1200px, min-height: 700px` 維持（已有）
- 元件各自檢查 breakpoints（多數已有彈性 grid/flex）
- 真的不能縮的元件用 `min-w-` 鎖死，超出 scroll
- **不做平板/手機 layout** — Tauri 桌面 only

---

## Development Methodology · TDD

每個元件 / utility / hook 採 **Red-Green-Refactor TDD**：

```
1. 紅 — 寫 .test.ts(x) 列出預期行為，部分 failing
2. 綠 — 寫實作直到 tests pass
3. 重構 + 視覺對齊 — 開 prototype html 對照，微調 CSS module 細節
4. Commit — 一個元件 / hook 一個 commit
```

### TDD 能抓的 vs 不能抓的

✅ **能**：行為契約、props、state 切換、事件處理、Manager pub/sub、
鍵盤快捷鍵、持久化、hook 卸載清理、type contracts

❌ **不能**：視覺像不像 H18、動畫流暢度、實際色彩對比、hover 視覺
feedback、responsive layout（jsdom 沒 layout）

→ 視覺對齊靠**眼睛 + prototype 對照**（每個 PR description 列出對照位置）

### 工具（已在 repo）

```
vitest + @testing-library/react + @testing-library/user-event
+ @testing-library/jest-dom + jsdom + @vitest/coverage-v8
```

### 覆蓋率目標（v0.7.0-alpha 期間不設 CI gate）

- Manager / utils / hooks: **90%+**
- Component: **70%+**（行為導向不追 line coverage）
- 不寫 snapshot tests（brittle）
- 不寫 E2E（之後）
- 不寫 visual regression（之後）

---

## Phase 0 · Foundation (~5-7 commits, 不是 1 個)

**Tag**: `0.7.0-alpha.1` 的開頭

Phase 0 是**整個 v0.7.0 的最底層**，要把 token 系統、theme 系統、
測試 infra 都立好才往上蓋。每個 step 一個 commit。

### Step 1 · TDD theme system 升級
**檔案**:
- `src/utils/theme.ts` (modified) — applyTheme 加 'system' 模式 +
  listen matchMedia 自動切
- `src/utils/__tests__/theme.test.ts` (new) — 6 tests
- `src/hooks/useTheme.ts` (new) — React hook 回 themeMode +
  effectiveTheme + setter
- `src/hooks/__tests__/useTheme.test.ts` (new) — 5 tests

完成後元件可以 `useTheme()`，不用 prop drilling。約 1-2 小時。

### Step 2 · Google Fonts 載入
**檔案**:
- `index.html` (modified) — 加 Google Fonts `<link>` (Inter /
  JetBrains Mono / Noto Sans TC / Fraunces / Noto Serif TC)

純 HTML 改動，無 test 需要。約 5 分鐘。

### Step 3 · CSS tokens 系統
**檔案**:
- `src/styles/tokens.css` (new) — `:root { --h18-* }` 兩個 theme
- `src/styles/tokens.ts` (new) — TS export 對應 CSS var 的 mapping
- `src/styles/__tests__/tokens.test.ts` (new) — 確保 ts/css 同步、
  TOKENS 物件結構正確
- `src/main.tsx` (modified) — import tokens.css

完成後 `:root` 有 H18 vars，但**沒元件用**所以視覺不變。約 1 小時。

### Step 4 · Global CSS reset + 基底
**檔案**:
- `src/styles/global.css` (new) — body / html base styles 用 H18 vars
- `src/index.css` (modified) — 改成只 import tokens.css + global.css；
  移除原本舊的 scrollbar 樣式

約 30 分鐘。

### Step 5 · CSS Modules type safety setup
**檔案**:
- `package.json` (modified) — 加 `typescript-plugin-css-modules` devDep
- `tsconfig.json` (modified) — plugins 加 typescript-plugin-css-modules
- `vite.config.ts` (verify) — Vite CSS Modules 預設已支援

完成後 IDE 在 `import s from './x.module.css'` 後 `s.` 會跳補全。
約 30 分鐘。

### Step 6 · AppSettings 加 appearance 欄位
**檔案**:
- `src/types/index.ts` (modified) — 加
  `appearance: { themeMode, density, fontSize, layout, toastStyle }`
- `src/services/storageService.ts` (modified) — getAppSettings 讀回時
  缺欄位自動填 default
- `src/services/__tests__/storageService.test.ts` (new) — TDD 確保
  舊 settings (沒 appearance 欄位) 升級正確

約 1 小時。

### Phase 0 完成驗證

- `npm test` 全 green
- `npm run dev` 跑起來，現有 UI **視覺完全不變**（沒元件用新 token）
- DevTools `:root` 有完整 `--h18-*` vars，切 .dark 全變
- `useTheme()` hook 可以在任何元件調用
- 舊 user 升級（沒 appearance 欄位的 settings）不會炸

**Phase 0 全部完成估約 4-6 小時 ÷ 6 commit**。

---

## Phase 1 · Chrome 替換 (3-5 commits)

**Tag**: `0.7.0-alpha.2`

把全應用都會看到的 chrome 換成 H18：top bar / 視窗控制 / 通知 / 對話框。

### 變更清單

| 動作 | 對應 prototype | 對應真實程式 |
|---|---|---|
| 新 `components/h18/TopBar.tsx` | `h18-parts.jsx` H18TopBar | 取代 MainWindow.tsx 內 `<header>` |
| 新 `components/h18/WindowControls.tsx` | `h18-parts.jsx` H18WindowControls | 加進 TopBar 左側；接 Tauri `getCurrentWindow().close()/.minimize()/.toggleMaximize()` |
| 重做 `components/ToastContainer.tsx` | `extras-toast.jsx` | 保留 toastService API；視覺改 card / typewriter 雙風格 + countdown bar |
| 重做 `components/ConfirmDialog.tsx` | `extras-confirm.jsx` | 保留 confirmService API；視覺改 H18 modal + ESC/Enter |
| 重做 `components/TaskIndicator.tsx` | `extras-task-indicator.jsx` | 保留 offlineQueueService 訂閱；視覺改 ☁ icon + dropdown |
| 改 `MainWindow.tsx` | h18-app.jsx 主 layout | 用新 TopBar + 加 system theme 狀態 + tweaks 持久化 |
| `tauri.conf.json` | — | `decorations: false` 讓我們自己畫 traffic lights（Tauri 2 syntax） |

### 風險

- **Tauri decorations**：關掉系統視窗框後，要自己處理 drag region (`data-tauri-drag-region`)，否則無法拖移
- **macOS native traffic lights**：Mac 用戶可能習慣原生位置；要 OS-detect？v0.7.0 alpha 先全平台用我們的，後續再看反饋
- **回退路徑**：若使用者反映新 chrome 有問題，需要 feature flag (`appearance.useH18Chrome`) 讓他們切回舊版？建議**不做** flag — alpha 就是要驗收新東西

---

## Phase 2 · Settings 重新組織 (2-3 commits)

**Tag**: `0.7.0-alpha.3`

對齊 H18 ProfilePage 的 8 sub-pane 結構。

### 變更清單

| 動作 | 對應 prototype |
|---|---|
| 重做 `components/SettingsView.tsx` | `h18-nav-pages.jsx` ProfilePage outer shell（左 sidebar 230px + 右 content） |
| 重做 `components/ProfileView.tsx` | 替換 placeholder 為 POverview A 變體（成就累積 + 登出按鈕） |
| 改 `settings/SettingsLocalTranscription.tsx` | 對齊 PTranscribe 視覺（Parakeet INT8/FP32 row + GPU detect + Fast/Standard） |
| 改 `settings/SettingsTranslation.tsx` | 對齊 PTranslate 視覺（Gemma 主 + Google 備 + CT2 fallback） |
| 改 `settings/SettingsCloudAI.tsx` + AIProviderSettings | 對齊 PCloud 5-provider grid + DEFAULT badge |
| 改 `settings/SettingsInterface.tsx` | 對齊 PAppearance（主頁佈局 SVG preview + 主題/密度/字體/Toast 風格） |
| 改 `settings/SettingsAudioSubtitles.tsx` | 視覺對齊 PAudio |
| 改 `settings/SettingsDataManagement.tsx` | 視覺對齊 PData |
| 改 `settings/SettingsAboutUpdates.tsx` | 視覺對齊 PAbout（含 Setup Wizard reset binding） |

### Phase 2 落地後

- 設定頁完全 H18 視覺
- 主題切換、字體大小、密度都接到實際 effects
- 主頁佈局選擇器存到 AppSettings 但**佈局本身還沒改**（Phase 3）

---

## Phase 3 · Setup Wizard + Login 重做 (2 commits)

**Tag**: `0.7.0-alpha.4`

獨立流程，不影響主應用，**先做這兩個風險最低**。

| 動作 | 對應 |
|---|---|
| 重寫 `components/SetupWizard.tsx` (+ SetupWizard.css 移除) | `extras-setup-wizard.jsx` 10-step flow |
| 重寫 `components/LoginScreen.tsx` | `extras-login.jsx` H18 樣式 |

兩個都已經是 H18 prototype 完整對應，幾乎照搬。

---

## Phase 4 · Conditional UI 視覺升級 (3-4 commits)

**Tag**: `0.7.0-alpha.5`

| 動作 | 對應 |
|---|---|
| 改 `components/RecoveryPromptModal.tsx` | `extras-recovery.jsx` |
| 加 `components/ErrorBoundary.tsx` 的 fallback UI | `extras-error-fallback.jsx` |
| 改 `components/VideoPiP.tsx` | `extras-video-pip.jsx`（已是 H18 風，主要校對） |
| 改 `components/UnofficialChannelWarning.tsx` | `extras-oauth.jsx` |
| 加 `components/AlignmentBanner.tsx` (新) + 串到 autoAlignmentService | `extras-alignment-banner.jsx` |

---

## Phase 5 · 主要頁面 Port (大工程，分多次)

**Tag**: `0.7.0-alpha.6` ~ `alpha.N`

主應用的「靈魂」— 一頁一個 commit，可隨時 ship。

### 順序建議（從低風險到高風險）

1. **CourseListView** → H18 風格課程卡片 + 新增/編輯 dialog
2. **CourseDetailView** → H18 hero (含 gradient + stats) + lecture list + grading + people 卡片
3. **NotesView** **review 模式** → 視覺對齊 H18 ReviewPage（不含 RVBilink concept hover、不含 chapter TOC）+ AudioPlayer 加上來
4. **NotesView** **recording 模式** → 視覺對齊 H18 RecordingPage layout A（單一 layout，不做 B/C）+ FinishingOverlay 結束過場
5. **AIChatPanel** → 視覺對齊 H18 AIDock 樣（保留 floating mode、移除 sidebar mode 設定）

### 備註

- NotesView 是 2870 行的巨獸，**不要重寫**，只**換視覺**：把 inline className 改成 H18 token，把 button/icon 改成 H18 樣
- 全部主頁面 port 完才升 `0.7.0-beta`

---

## Defer to v0.7.x patches / v0.8

設計裡有但這版不做的：

- Inbox（需要 schema）
- Calendar with weekly schedule（需要 schema）
- ⌘K Command Palette（需要 search index）
- AIDock 浮動 ⌘J（重做 AI 架構）
- AI Page 全螢幕（chat history sidebar）
- RVBilink concept hover tooltips（需要 concept extraction）
- NotesEditor canvas 模式（需要繪圖層）
- POverview B/C/D variants（先 A 就好）
- iPad mirror（NotesEditor 內的浮動 iPad）

---

## 風險區與決策點

### R1 · Tailwind v3 動態值
Tailwind v3 部分 utility 不支援 `var()`。對策：用 arbitrary value 語法或保留 inline style。**先試試看，必要時升 Tailwind v4**（v4 改善了 var 支援）。

### R2 · 元件 prop drilling
原本元件不傳 T (theme)，因為用 Tailwind dark: prefix。新元件要不要也走 dark: pattern？**走** — 比 prop drilling theme 物件乾淨。

### R3 · AppSettings migration
新增 `appearance.{themeMode, density, fontSize, layout, toastStyle}` 欄位。舊 user 升級時要有 default。對策：
- storageService 讀 settings 時，缺欄位自動填 default
- 不需 DB migration script（純前端 JSON）

### R4 · 既有元件混用 Tailwind dark: 跟 H18 vars
過渡期會有元件還用 `dark:bg-slate-800` (舊)、有的用 `bg-h18-surface` (新)。對策：
- Phase 0 同時定義「舊→新」mapping table（PR 描述裡寫明）
- 每個 PR 標記它換了哪些元件
- 直到所有 component 都換完才升 beta

### R5 · 測試影響
- vitest 元件測試多用 className 斷言
- UI 大改後**測試斷言要全部審視**
- 對策：每個 PR 把對應 test snap update

### R6 · CI 影響
- Tailwind config 改可能影響 production build size
- CSS vars 在 IE 不支援 — 我們不支援 IE，OK
- WebView2 (Windows) / WebKit (Mac) 都支援 → OK

### R7 · 平台差異 (traffic lights)
- macOS 原生左上、Windows 原生右上、Linux 看 DE
- 設計用 macOS 風放左上 — 對 Windows 用戶會覺得「位置怪怪的」
- **決策：v0.7.0 alpha 全平台統一放左上**，理由：跨平台一致性 > 平台原生
- 若反饋強烈再加 OS-detect（v0.7.x patch）

---

## 執行順序速覽

| Alpha | Phase | 工作量 | 風險 |
|---|---|---|---|
| `0.7.0-alpha.1` | Phase 0 (Foundation) | 1 day | 低 |
| `0.7.0-alpha.2` | Phase 1 (Chrome) | 3-5 days | 中（Tauri decorations） |
| `0.7.0-alpha.3` | Phase 2 (Settings) | 3-5 days | 低 |
| `0.7.0-alpha.4` | Phase 3 (Setup + Login) | 2-3 days | 低 |
| `0.7.0-alpha.5` | Phase 4 (Conditional UI) | 3-4 days | 中 |
| `0.7.0-alpha.6+` | Phase 5 (主頁面 rolling) | 1-2 weeks | 高 (NotesView 大) |
| `0.7.0-beta` | 全部測試 + 修 | — | — |
| `0.7.0` | stable | — | — |

每個 alpha 結束就可以 ship 給少數人試。

---

## 開始的第一步

**Phase 0 三件事**：
1. `index.html` 加 Google Fonts link
2. 建 `src/styles/h18-tokens.css` 注入 vars
3. `tailwind.config.js` extend colors

這三件做完不會影響任何現有畫面（因為還沒元件用新 token），可以放心 ship。

---

## Branch / Commit 策略

- 工作 branch：`feat/h18-design-snapshot` (此 worktree)
- 每個 Phase 完整可運作後 commit
- 每 N 個 commits push 一次 + 開 PR review
- 合回 main 後 tag `v0.7.0-alpha.X`
- 不單獨開 sub-branch（Phase 0/1/2 連續累積）

---

## Human Checkpoints · 人類檢查點協定

執行 v0.7.0 是長期工程，需要**定期人工 review**。我會在以下 checkpoint
**強制停下來等你 review**，不會擅自跨過。

### Hard Checkpoints (必停)

每個 CP 都會：
1. 工作完整 commit (含當下 status)
2. 最後一個 commit 訊息 prefix `[CP-X]` 並列「下一個 CP 是什麼 + 待你
   review 什麼」
3. 我**停止所有後續工作**直到你給綠燈
4. 給綠燈方式：直接告訴我「continue」或「過 CP-X，繼續 CP-Y」

| Checkpoint | 觸發點 | Review 重點 | 預計工時 |
|---|---|---|---|
| **CP-0 · Pre-flight** | npm install + vitest 跑得起來 + 看一遍 plan 沒問題 | 工作環境準備好、計畫沒疑問 | 10 分鐘 |
| **CP-1 · Phase 0 done** | Foundation 全部 6 step 完成 (theme system / tokens / global.css / CSS Modules type / AppSettings migration) | 視覺**不變**但 token 系統能用、useTheme 可調用、所有新 test pass | 4-6 小時 |
| **CP-2 · Phase 1 done** | Chrome 換好 (TopBar / WindowControls / Toast / Confirm / TaskIndicator) | **第一次大視覺變動**：top bar 換 H18 樣、視窗控制接 Tauri | 3-5 天 |
| **CP-3 · Phase 2 done** | Settings 重新組織完成 (8 sub-pane 對齊 H18 ProfilePage) | 設定頁全 H18 樣、tweaks bindings 真的生效 | 3-5 天 |
| **CP-4 · Phase 3 done** | SetupWizard + LoginScreen 重做完成 | 兩個獨立流程改完，可以直接把使用者引到新版 | 2-3 天 |
| **CP-5 · Phase 4 done** | Conditional UI 全部視覺升級 (Recovery / Error / VideoPiP / Unofficial / + 新 AlignmentBanner) | 條件 UI 全 H18 樣 | 3-4 天 |
| **CP-6 · Phase 5 第 1 頁** | CourseListView H18 化 | 第一個主頁面換好 | 1-2 天 |
| **CP-7 · Phase 5 第 2 頁** | CourseDetailView H18 化 | | 1-2 天 |
| **CP-8 · Phase 5 第 3 頁** | NotesView review 模式 H18 化 + AudioPlayer | NotesView 是 2870 行巨獸，**最高風險** | 3-5 天 |
| **CP-9 · Phase 5 第 4 頁** | NotesView recording 模式 H18 化 | 跟 CP-8 共用大量 code，預期較快 | 2-3 天 |
| **CP-10 · Phase 5 第 5 頁** | AIChatPanel H18 化 | 最後一塊主頁面 | 1-2 天 |
| **CP-FINAL · v0.7.0-beta** | 全部 Phase 完成 + 整合測試 + 移除 Tailwind + 修最後一輪 bug | 升 beta 前最後 review | 1 週 |

### Soft Checkpoints (我會主動暫停問你)

只有需要**你的判斷**才會停，不是時間到就停。觸發條件：

- **架構決策**：plan 沒寫的設計問題（例：發現某元件需要新 backend API）
- **scope creep**：發現某 feature 比預期大很多（例：NotesView 的 sub-component
  其實要全部重寫）
- **trade-off 衝突**：兩個原則打架且 plan 沒覆蓋
- **plan 偏離**：執行中發現 plan 寫的東西實際做不到 / 太貴
- **不可逆動作**：force push、刪檔、改 git history 等 — 永遠停下問你

### 緊急停止 (Hard stop)

以下狀況我會**立即停下**並把現況落地成可恢復狀態：

- Tests 大規模 fail (>30%) 且修不回來
- Production build 跑不起來 (`npm run build` 失敗)
- Tauri build 失敗
- Git 衝突我無法乾淨解掉
- 工作環境壞掉 (vitest 跑不起、TS check 失效等)

緊急停止後我會 commit 一個 `[STOP]` 開頭的 commit，列：
- 發生什麼
- 我已嘗試什麼
- 你需要做什麼
- 怎麼回到上一個 working state

### 進度匯報

每個 Hard CP 達成時我會在 chat 給你：
- ✅ 完成的事 (commits 列表 + 一句話描述)
- 📊 測試 / 覆蓋率現況
- 🔍 視覺驗證建議 (打開哪一頁看哪裡)
- ⚠️ 任何意外發現 / 需注意的事
- ▶ 下一個 CP 的內容預覽

### 一個 CP 內可以做的事

CP 之間我**不需要任何中途確認**，可以連續執行：
- 寫 test → 寫 implementation → refactor → commit
- 串多個 commits 達成同一個 CP
- 處理小的 bug / typo / formatting
- 寫文件、註解、commit message

### 一個 CP 內**不**可以做的事

- 跨 phase 工作 (CP-1 內不能開始做 CP-2 的內容)
- 修改 plan doc 主結構 (小 typo OK，章節改動要等 CP)
- 引入計畫沒提到的依賴 (`npm install` 新 lib 要等 CP)
- 改動 `feat/h18-design-snapshot` 以外的 branch
- 動 worktree 設定 / git config

### CP 之外的紀律

每個 commit message 都會包含：
- prefix `feat(h18-{phase})` / `test(h18-{phase})` / `docs(h18)` / `chore(h18)`
- 一行 summary
- 詳細 body 說明改了什麼、為什麼
- 當下測試狀況 (X tests pass)
- 視覺對照 prototype 哪段
